#import "@preview/theorion:0.5.0": *
#import cosmos.fancy: *
#show: show-theorion

= Foliation

== Definations and notations

#definition[
For any sheaves $cal(M)$ and $cal(N)$ on a normal variety $X$, we denote $(cal(M) times.o cal(N))^(**)$ and $(cal(M)^(times.o n ))^(**)$ as $M times.square cal(N)$ and $cal(M)[n]$, respectively.
]<def:reflex-sheaves>

In @def:reflex-sheaves

